import React from 'react'
import { Navbar } from 'react-bootstrap'

const Header = () => (
  <Navbar inverse>
    <Navbar.Header>
      <Navbar.Brand>
        React-Bootstrap
      </Navbar.Brand>
    </Navbar.Header>
  </Navbar>
)


export default Header
